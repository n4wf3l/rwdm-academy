# Configuration API pour la production
# Décommentez et définissez l'URL de votre API en production si elle est sur un domaine différent du frontend
# VITE_API_URL=https://votre-api-domain.com

# Si cette variable n'est pas définie, l'application utilisera des chemins relatifs (/api/...)
# Cela suppose que le frontend et le backend sont sur le même domaine/origine